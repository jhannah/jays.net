In July 2023 I was jacking around with using Ansible to install 15 websites.
Instead I moved almost everything to be hosted on Netlify.

ansible all -m ping
ansible all -a "uname -a"

point your apt sources at stable and doing `apt-get update && apt-get full-upgrade` every week or so?
  https://dementedandsadbut.social/@genehack/109853562299259360

